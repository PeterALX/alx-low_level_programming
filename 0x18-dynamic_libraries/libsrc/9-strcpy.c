#include "main.h"

/**
* _strcpy - copy string from one buffer to another
* @dest: the location to copy into
* @src:  the location to copy from
* Return: void
*/
char *_strcpy(char *dest, char *src)
{
	char *j = dest;

	while
		((*dest++ = *src++));
	return (j);
}
