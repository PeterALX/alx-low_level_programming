#include <stdio.h>

/**
* _abs - compute the absolute value of a number
* @k: the number  whose absolute value os to be found
* Return: the absolut value of k
*/
int _abs(int k)
{
	if (k > 0)
		return (k);
	return (-k);
}

